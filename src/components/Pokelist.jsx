import { Link } from "react-router-dom";
const Pokelist = () => {
    return (
        <div>
            Pokelist
            <Link to="/pokemon">single pokemon</Link>
        </div>
    );
};

export default Pokelist;
